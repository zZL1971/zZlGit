<#assign types={"width":"int", "filterable":"boolean", "selectOnFocus":"boolean"}/><#compress>[<#list result as map>{<#list map?keys as itemKey><#if map[itemKey]?is_hash_ex>${itemKey}:[{<#list map[itemKey]?keys as childKey><#if childKey=='handler'>${childKey}:function(grid,rowIndex,colIndex){this.up('grid').fireEvent('${map[itemKey][childKey]}',grid,rowIndex,colIndex);}<#else><#if types[childKey]?exists>${childKey}:${map[itemKey][childKey]}<#else>${childKey}:'${map[itemKey][childKey]}'</#if></#if><#if childKey_has_next>,</#if></#list>}]<#else><#if types[itemKey]?exists>${itemKey}:${map[itemKey]}<#else>${itemKey}:'${map[itemKey]}'</#if></#if><#if itemKey_has_next>,</#if></#list>}<#if map_has_next>,</#if></#list>]</#compress>